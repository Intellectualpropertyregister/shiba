<h1>Listing cities</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>State</th>
      <th>Country</th>
      <th>Lowerleftlat</th>
      <th>Lowerleftlng</th>
      <th>Upperrightlng</th>
      <th>Upperrightlng</th>
      <th>Additionalinfo</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @cities.each do |city| %>
      <tr>
        <td><%= city.name %></td>
        <td><%= city.state %></td>
        <td><%= city.country %></td>
        <td><%= city.lowerLeftLat %></td>
        <td><%= city.lowerLeftLng %></td>
        <td><%= city.upperRightLng %></td>
        <td><%= city.upperRightLng %></td>
        <td><%= city.additionalInfo %></td>
        <td><%= link_to 'Show', city %></td>
        <td><%= link_to 'Edit', edit_city_path(city) %></td>
        <td><%= link_to 'Destroy', city, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New City', new_city_path %>
